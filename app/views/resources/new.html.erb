<h1>New resource</h1>

<%= error_messages_for :resource %>

<% form_for(@resource, :html => { :multipart => true }) do |f| %>

<%= render :partial => 'form', :locals => { :form => f, :label_text => "Create" } %>

<% end %>

<%= link_to 'Back', resources_path %>
